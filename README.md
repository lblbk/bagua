# bagua

```
# 1. 创建 Vite 项目 (选择 React + JavaScript/SWC)
npm create vite@latest coin-toss -- --template react

# 2. 进入项目目录
cd coin-toss

# 3. 安装依赖
npm install

# 4. 安装 Tailwind CSS 及其依赖
npm install -D tailwindcss postcss autoprefixer

# 5. 初始化 Tailwind 配置
npx tailwindcss init -p
```